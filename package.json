{
  "name": "yadal-dev",
  "version": "0",
  "private": true,
  "scripts": {
    "build:all": "lerna run build",
    "watch": "tsc -w",
    "build": "tsc",
    "test": "lerna run build && c8 node --test"
  },
  "type": "module",
  "workspaces": [
    "packages/*",
    "."
  ],
  "devDependencies": {
    "@types/node": "^20.5.7",
    "@types/ws": "^8.5.5",
    "c8": "^8.0.1",
    "lerna": "^7.2.0",
    "typescript": "^5.2.2",
    "websocket": "^1.0.34",
    "ws": "^8.13.0"
  },
  "dependencies": {
    "yadal": "workspace:^"
  },
  "publishConfig": {
    "access": "public"
  }
}